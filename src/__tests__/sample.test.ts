/**
 * ✅ Sample Jest Test (`src/__tests__/sample.test.ts`)
 * -------------------------------------
 * This file contains a **basic Jest test** to ensure that the
 * testing setup is functioning correctly.
 *
 * 🚀 Why Use This Test?
 * - Confirms that Jest is installed and running properly.
 * - Serves as an example for writing unit tests.
 *
 * 🛠️ Technologies Used:
 * - **Jest** → JavaScript testing framework.
 */

describe('Sample Test', () => {
  it('should pass', () => {
    expect(true).toBe(true) // ✅ Basic test: Ensures `true` is `true`
  })
})
